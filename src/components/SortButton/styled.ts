import styled from 'styled-components'

export const SortWrapper = styled.div`
  display: flex;
  gap: 10px;
  margin-bottom: 20px;

  label {
    display: flex;
    align-items: center;
    gap: 5px;
    cursor: pointer;
  }
`
